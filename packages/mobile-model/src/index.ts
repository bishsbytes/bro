/**
 * Persistence-independent records used by the mobile application.
 *
 * Repository implementations consume and return these shapes, but this
 * package deliberately contains no database, Expo, network, or React code.
 */
export * from "./records";
