services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-bro}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bro_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-bro}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - bro_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  bro_pgdata:
